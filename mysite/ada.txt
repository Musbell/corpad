fh?a+V24;E]wPk@a


ssh root@164.90.216.229
ssh nick@164.90.216.229
su nick
cd ~/adashi/


sudo systemctl daemon-reload


   sudo systemctl restart gunicorn

sudo systemctl restart nginx

CREATE DATABASE adashidb;

 CREATE USER adashiadmin WITH PASSWORD 'password';

 ALTER ROLE adashiadmin SET client_encoding TO 'utf8';

  ALTER ROLE adashiadmin SET default_transaction_isolation TO 'read committed';

  ALTER ROLE adashiadmin SET timezone TO 'UTC';

  GRANT ALL PRIVILEGES ON DATABASE adashidb TO adashiadmin;

sudo certbot --nginx -d corpad.ng -d www.corpad.ng

[Unit]
Description=gunicorn daemon
Requires=gunicorn.socket
After=network.target

[Service]
User=nick
Group=www-data
WorkingDirectory=/home/nick/adashi
ExecStart=/home/nick/adashi/env/bin/gunicorn \
          --access-logfile - \
          --workers 3 \
          --bind unix:/run/gunicorn.sock \
          mysite.wsgi:application

[Install]
WantedBy=multi-user.target


sudo nano /etc/nginx/sites-available/adashi


server {
    listen 80;
    server_name www.corpad.ng corpad.ng 164.90.216.229;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/nick/adashi;
    }

    location /media/ {
        root /home/nick/adashi;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}

sudo ln -s /etc/nginx/sites-available/adashi /etc/nginx/sites-enabled
